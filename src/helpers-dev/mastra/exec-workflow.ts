// import { mastra } from "../../mastra";

// const workflow = mastra.vnext_getWorkflow('getEMAWorkflow')
// const run = workflow.createRun()

// // Start the workflow with Lagos as the location
// const result = await run.start({ inputData: { symbol: "BTCUSDT", timeframe: "1h", limit: 100 } })
// console.dir(result, { depth: null })